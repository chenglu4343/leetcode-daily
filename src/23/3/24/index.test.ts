import { describe, it, expect } from 'vitest'
import { StreamChecker } from '.'

describe.todo('23-3-24', () => {
    it('example-1', () => {
        const obj = new StreamChecker(["cd", "f", "kl"])

        // expect(obj.query('a')).toBe(false)
        // expect(obj.query('cd')).toBe(true)
    })
})